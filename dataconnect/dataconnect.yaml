specVersion: "v1"
serviceId: "matador-reservations"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "matador-reservations-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
